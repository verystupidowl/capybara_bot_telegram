package ru.tggc.botapp;

//@SpringBootTest
public class BotAppTest {
//    @Autowired
//    private FormatService formatService;
//
//    @Test
//    public void test_all_keys_exists() {
//        Arrays.stream(AdminMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(CasinoMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(CommonMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(ErrorMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(FightMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(InfoMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(RaceMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//        Arrays.stream(WorkMsgKey.values()).forEach(key -> Assertions.assertTrue(formatService.exists(key)));
//    }
}
